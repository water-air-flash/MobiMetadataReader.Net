using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace MobiMetadataReader.Net
{
    public enum eExplorerButton
    {
        AddShortcut,
        Backward,
        Forward,
        ParentDirectory,
        Refresh,
        Home,
        ViewMetadata,
        Configure
    }

    public enum eSortType
    {
        String,
        Number,
        DateTime
    }
}
